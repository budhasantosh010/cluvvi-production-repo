export function evaluateCommand(): void {
  console.log(
    "C0 produces fixture-only artifacts and is not eligible for lead-quality evaluation.",
  );
  console.log(
    "Human opportunity evaluation begins after real discovery produces source-backed candidates.",
  );
}
