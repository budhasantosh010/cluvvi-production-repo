{
  "name": "@cluvvi/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "start:once": "tsx src/index.ts --once",
    "build": "tsup src/index.ts --format esm --platform node --target node24 --clean",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cluvvi/config": "workspace:*",
    "@cluvvi/core": "workspace:*",
    "@cluvvi/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "tsup": "8.5.1",
    "tsx": "4.23.1",
    "typescript": "5.9.3"
  }
}
